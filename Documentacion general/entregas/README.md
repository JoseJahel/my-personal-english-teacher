# Entregas de aula

## Avance 2 — presentación

- Archivo: [`avance-2-presentacion.pptx`](./avance-2-presentacion.pptx)
- Duración: 10–15 min (notas del presentador en cada diapositiva)
- Demo: solo localhost (`pnpm build && pnpm preview`)
- Plan B: `pnpm build:ensayo` y `#practice-mock`

Regenerar (opcional):

```text
cd "Documentacion general/entregas"
npm install
node build-avance-2-presentacion.js
```

Las capturas de `capturas/` están embebidas en el `.pptx`.
